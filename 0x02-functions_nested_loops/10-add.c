#include "main.h"
/**
 * add - adds two integers
 * @n : contains first value
 * @x : contains second value
 * Return:0
 */
int add(int n, int x)
{
	int result;
		result = n + x;
		return (result);
}
